#include "util.h"

/*------------------------Case 1------------------------*/
const int case1N = 3;
int case1ReturnSize; //can't be const, it needs to be changed by the solution method in the problem.c file
const char* case1Output[] = {"1", "2", "Fizz"};
/*------------------------Case 2------------------------*/
const int case2N = 5;
int case2ReturnSize; //can't be const, it needs to be changed by the solution method in the problem.c file
const char* case2Output[] = {"1", "2", "Fizz", "4", "Buzz"};
/*------------------------Case 3------------------------*/
const int case3N = 15;
int case3ReturnSize; //can't be const, it needs to be changed by the solution method in the problem.c file
const char* case3Output[] = { "1", "2", "Fizz", "4", "Buzz", "Fizz", "7", "8",
                        "Fizz", "Buzz", "11", "Fizz", "13", "14", "FizzBuzz"};
